- Clone the project:
  
        git clone
- Install dependencies:
  
        comoser install
        npm install
- SetUp Database:

        bin/console d:d:c
        bin/console d:m:m
- Run Application:

        symfony serve -d
        npm run watch